CREATE TABLE blogful_articles (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT NOT NULL,
  -- TIMESTAMPZ is shorthand for TIMESTAMP WITH TIME ZONE
  date_published TIMESTAMPTZ DEFAULT now() NOT NULL,
  content TEXT
);



